import { ArticleDetails } from "./ArticleDetails/feature";
import { CommentList } from "./CommentList/feature";
import { CommentInput } from "./CommentInput/feature";

export const Features = {
  ArticleDetails,
  CommentList,
  CommentInput,
};
