import Button from "@/components/common/Button";

export default function LoginTest() {
  
  return (
    <div>
      <div>이곳에서 로그인 기능을 이용할 수 있습니다.</div>
      <Button name="로그인" disabled={false}/>
    </div>
  )
}